package com.example.model

enum class CameraMode(val title: String, val subtitle: String) {
    PHOTO("PHOTO", "High-quality vintage photo capture"),
    VIDEO("VIDEO", "Retro video recording with live effects"),
    PRO("PRO", "Manual exposure, ISO, focus & RAW workspace")
}
